namespace Pessoa.Models

{
    // Define um record chamado PessoaRequest que contém uma propriedade Nome
    // Record é um tipo de referência que fornece uma maneira concisa de definir um objeto imutável com suporte para igualdade de valor
    public record PessoaRequest(string Nome);
}
